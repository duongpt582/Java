

interface Insured {
    double getInsuranceValue(int year); // dùng để tính giá trị bảo hiểm

    double getInsurancePremium(int year); // dùng để tính chi phí bảo hiểm

    void outputPolicyDetails(int year); // dùng để in ra thông tin liên quan
}

